export type UpComing = {
  title: string;
  rating: number;
  imageUrl: string;
};
export type Props = {
  text: string;
  title: string;
  description: string;
  imageUrl: string;
  rating: number;
};

export type Movie = {
  text: string;
  title: string;
  description: string;
  imageUrl: string;
  rating: number;
};

export type PopularMovies = {
  title: string;
  rating: number;
  imageUrl: string;
};

export type topRated = {
  title: string;
  rating: number;
  imageUrl: string;
};

export type Wicked = {
  title: string;
  minute: any;
  imageUrl: string;
};

export type MoreLike = {
  title: string;
  rating: number;
  imageUrl: string;
};
