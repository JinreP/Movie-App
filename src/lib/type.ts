export type PopularProps = {
  title: string;
  rating: number;
  imageUrl: string;
};
export type Props = {
  text: string;
  title: string;
  description: string;
  imageUrl: string;
  rating: number;
};

export type Movie = {
  text: string;
  title: string;
  description: string;
  imageUrl: string;
  rating: number;
};
